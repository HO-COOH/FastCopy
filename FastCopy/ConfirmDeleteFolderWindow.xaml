<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="FastCopy.ConfirmDeleteFolderWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:essential="using:WinUI3Package"
    xmlns:local="using:FastCopy"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Uid="ConfirmDeleteFolderWindow"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <essential:MicaBackdropWithFallback>
            <essential:MicaBackdropWithFallback.Fallback>
                <DesktopAcrylicBackdrop />
            </essential:MicaBackdropWithFallback.Fallback>
        </essential:MicaBackdropWithFallback>
    </Window.SystemBackdrop>

    <Grid>
        <Grid.Resources>
            <essential:NonMaximizableWindowWorkaround
                x:Name="NonMaximizableWorkaround"
                IsMaximizable="False"
                Window="{x:Bind}" />
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Image x:Name="RecycleBinIcon" Style="{StaticResource RecycleBinIconStyle}" />

        <Grid Grid.Column="1" Style="{StaticResource ConfirmDeleteWindowContentGridStyle}">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
                <RowDefinition Height="Auto" />
            </Grid.RowDefinitions>

            <TextBlock
                x:Uid="ConfirmDeleteFolderTitleTextBlock"
                FontSize="16"
                FontWeight="SemiBold"
                TextWrapping="Wrap" />

            <Grid Grid.Row="1">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="*" />
                </Grid.ColumnDefinitions>

                <Image
                    x:Name="FolderIcon"
                    Width="48"
                    Height="48"
                    Margin="0,0,12,0" />
                <TextBlock
                    Grid.Column="1"
                    VerticalAlignment="Center"
                    Text="{x:Bind FolderName}"
                    TextWrapping="Wrap" />
            </Grid>

            <StackPanel
                Grid.Row="2"
                HorizontalAlignment="Right"
                Orientation="Horizontal"
                Spacing="8">
                <Button
                    x:Name="YesButton"
                    x:Uid="YesButton"
                    MinWidth="80"
                    Click="YesButton_Click"
                    Style="{StaticResource AccentButtonStyle}" />
                <Button
                    x:Name="NoButton"
                    x:Uid="NoButton"
                    MinWidth="80"
                    Click="NoButton_Click" />
            </StackPanel>
        </Grid>
    </Grid>

</Window>
