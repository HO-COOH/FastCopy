<?xml version="1.0" encoding="utf-8" ?>
<Window
    x:Class="FastCopy.ConfirmDeleteMultipleItemsWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:essential="using:WinUI3Package"
    xmlns:local="using:FastCopy"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    x:Uid="ConfirmDeleteMultipleItemsWindow"
    mc:Ignorable="d">

    <Window.SystemBackdrop>
        <essential:MicaBackdropWithFallback>
            <essential:MicaBackdropWithFallback.Fallback>
                <DesktopAcrylicBackdrop />
            </essential:MicaBackdropWithFallback.Fallback>
        </essential:MicaBackdropWithFallback>
    </Window.SystemBackdrop>

    <Grid Style="{StaticResource ConfirmDeleteWindowContentGridStyle}">
        <Grid.Resources>
            <essential:NonMaximizableWindowWorkaround
                x:Name="NonMaximizableWorkaround"
                IsMaximizable="False"
                Window="{x:Bind}" />
        </Grid.Resources>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>

        <Image x:Name="RecycleBinIcon" Style="{StaticResource RecycleBinIconStyle}" />

        <TextBlock
            Grid.Column="1"
            FontSize="16"
            FontWeight="SemiBold"
            TextWrapping="Wrap">
            <Run x:Uid="ConfirmDeleteMultipleItemsRun1" />
            <Run Text="{x:Bind ItemCountText}" />
            <Run x:Uid="ConfirmDeleteMultipleItemsRun2" />
        </TextBlock>

        <StackPanel
            Grid.ColumnSpan="2"
            HorizontalAlignment="Right"
            VerticalAlignment="Bottom"
            Orientation="Horizontal"
            Spacing="8">
            <Button
                x:Name="YesButton"
                x:Uid="YesButton"
                MinWidth="80"
                Click="YesButton_Click"
                Style="{StaticResource AccentButtonStyle}" />
            <Button
                x:Name="NoButton"
                x:Uid="NoButton"
                MinWidth="80"
                Click="NoButton_Click" />
        </StackPanel>
    </Grid>
</Window>
